# Промежуточные данные (генерируются при каждом запуске)
data/raw_news.json
data/analyzed_news.json

# История публикаций НЕ игнорируем — она нужна для защиты от повторов между запусками

# Python
__pycache__/
*.pyc

# Секреты (НИКОГДА не коммитить!)
.env